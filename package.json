{
    "name" : "yeet-for-cheats",
    "license" : "",
    "engines" : {
        "node" : "12.9.1",
        "yarn" : "1.19.1"
    },
    "scripts": {
        "build" : "echo '---yarn install---';yarn --cwd frontend install && yarn --verbose --cwd frontend build",
        "deploy" : "echo '---yarn deploy---';cp -R node_modules frontend/node_modules;cp -a frontend/build/. public/",
        "gen_key" : "openssl genrsa -out config/keys/private_key.key 8192",
        "heroku-postbuild": "yarn --verbose run build && yarn --verbose deploy;yarn --verbose gen_key"
    }
}